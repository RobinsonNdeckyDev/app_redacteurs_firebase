import 'package:flutter/material.dart';

class UpdateRedacteur extends StatelessWidget {
  const UpdateRedacteur({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
